<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Common control's style -->
    <Style x:Key="ControlStyle" TargetType="{x:Type Control}">

        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource Primary-Foreground-100-Brush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>

        <Setter Property="BorderThickness" Value="0" />

        <Setter Property="FocusVisualStyle" Value="{x:Null}" />

        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />

        <Setter Property="IsTabStop" Value="False" />

        <Setter Property="MinHeight" Value="{DynamicResource MinimumControlHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource MinimumControlWidth}" />
        <Setter Property="Height" Value="{DynamicResource MinimumControlHeight}" />

        <Setter Property="Padding" Value="{DynamicResource SmallThickness}" />

        <Setter Property="FontFamily" Value="{DynamicResource Font-1-Regular}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="SnapsToDevicePixels" Value="false" />

    </Style>

</ResourceDictionary>